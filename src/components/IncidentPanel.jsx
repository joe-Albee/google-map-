import { ArrowUpRight, CalendarDays, MapPinned } from 'lucide-react'
import StatusPill from './StatusPill'
export default function IncidentPanel({ incidents, onViewAll }) { return <section className="panel incidents-panel"><div className="panel-heading"><div><div className="eyebrow">HISTORY NEAR YOUR ROUTE</div><h2>Reported incidents <span className="count-badge">{incidents.length}</span></h2></div><button className="text-button" onClick={onViewAll}>View all <ArrowUpRight size={15} /></button></div>{incidents.length === 0 && <p className="empty-services">No historical incidents found near this route.</p>}{incidents.slice(0, 3).map((item) => <div className="incident-row" key={item.id}><div className={`incident-symbol ${item.type}`}><MapPinned size={17} /></div><div className="incident-copy"><div><strong>{item.type.replace('_', ' ').toUpperCase()}</strong><StatusPill level={item.severity === 'HIGH' ? 'HIGH' : item.severity === 'MEDIUM' ? 'MEDIUM' : 'LOW'}>HISTORY</StatusPill></div><span>{item.locationName}</span><small><CalendarDays size={12} /> {item.incidentDate}</small><small>{item.description}</small><a href={item.sourceUrl} target="_blank" rel="noreferrer">View source</a></div></div>)}</section> }
