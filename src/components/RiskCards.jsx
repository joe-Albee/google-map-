import { Droplets, Mountain, OctagonAlert, TriangleAlert } from 'lucide-react'
import StatusPill from './StatusPill'
export default function RiskCards({ risk, loading }) { const flood = risk.counts.flood || 0; const landslide = (risk.counts.landslide || 0) + (risk.counts.rockslide || 0); const cards = [{ label: 'Flood risk', icon: Droplets, count: flood, detail: flood ? `${flood} historical report${flood > 1 ? 's' : ''} nearby` : 'No historical reports found' }, { label: 'Landslide risk', icon: Mountain, count: landslide, detail: landslide ? `${landslide} historical report${landslide > 1 ? 's' : ''} nearby` : 'No historical reports found' }, { label: 'Road condition', icon: OctagonAlert, count: 0, detail: 'Live road incident data unavailable' }]; return <section className="risk-grid">{cards.map(({ label, icon: Icon, count, detail }) => <div className="risk-card" key={label}><div className="risk-icon"><Icon size={18} /></div><span>{label}</span><strong>{loading ? 'Checking route…' : detail}</strong><StatusPill level={count ? 'MEDIUM' : 'LOW'}>{count ? 'CAUTION' : 'NORMAL'}</StatusPill></div>)}</section> }
